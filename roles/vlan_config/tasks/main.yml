---
# tasks file for roles/vlan_config
- name: Configure VLANs
  ios_config:
    src: vlan.j2
#  when: vtp_mode[inventory_hostname] == "server"
  notify: Save config




#- name: Configure VLANs
#   ios_vlans:
 #    config:
 # ios_config:
 #    config:
  #  src: vlan.j2
#      - vlan_id: "{{ item.id }}"
 #       name: "{{ item.name }}"
  #loop: "{{ vlans }}"
  #notify: Save config

